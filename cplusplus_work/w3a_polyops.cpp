
// -----------------------------------------------------------
// AUTHOR Alejandro Aparicio aaparici@bu.edu
// Part A: Polynomial Operations
// -----------------------------------------------------------


#include <iostream>  // REMOVE THIS LIBRARY BEFORE SUBMITTING
#include <iomanip>   // REMOVE THIS LIBRARY BEFORE SUBMITTING
#include <vector> // vectors are indexable and variable size.

using namespace std;

typedef vector<double> Poly;

// Add two polynomials, returning the result
Poly add_poly(const Poly &a,const Poly &b);
{
  vector<double> newvec;

  //receive a vector of any size and transform it into a polynomial
  cout << "vector size: " << a.size() << endl;
  cout << "vector size: " << b.size() << endl;
  if (a.size < b.size);
  {
    v = b.begin();
    for (i=0; i<)
    {
      addition = 
      newvec.push_back(addition)
    }  
  }
  

}

// Multiply two polynomials, returning the result.
// Poly multiply_poly(const Poly &a,const Poly &b);
// {

// }


// -----------------------------------------------------------
int main()
{

  

}